# 任务文本（Task Topology Check 的拓扑输入信号 — data-neutral 合成例）

用户指令原文（本示例不含任何真实业务细节，仅供拓扑信号演示）：

> 本业务包含 **3 个系列**，**每个系列一个**输出表：系列一、系列二、系列三。
> 三个系列使用同一源工作簿与同一客户模板，源数据按系列分 sheet 存放。

Topology Check 只看文本中的拓扑信号 ——「3 个系列」「每个系列一个」——
即足以规划 3 条 run（shared_source / shared_template 由 run 清单派生）；
**不需要理解任何业务细节**（业务专用术语、符号、换算规则等一律不读、不推导）。

对应骨架见同目录 `task_topology_3runs.yaml`（3 条 run，与三个系列一一对应）。